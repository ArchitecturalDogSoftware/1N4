[package]
name = "ina-threading"
description = "Provides concurrency solutions for 1N4."
authors = ["Jaxydog"]
categories = ["asynchronous", "concurrency"]
keywords = ["channels", "threads"]

version = "0.1.0"

edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
default = []

[dependencies]
thiserror = "2.0"
tokio = { version = "1.41", features = ["rt", "sync", "time"] }
